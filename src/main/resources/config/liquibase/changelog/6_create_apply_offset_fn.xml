<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.5.xsd">

    <changeSet id="6_create_apply_offset_fn.xml-1" author="me">
    <createProcedure dbms="postgresql"
                     encoding="UTF-8"
                     procedureName="apply_offset"
                     relativeToChangelogFile="true"
                     schemaName="public"
    >
        CREATE OR REPLACE FUNCTION apply_offset(time_val timestamp, gmt_offset real)
            RETURNS timestamp AS $$
        BEGIN
            RETURN time_val + INTERVAL '1 minute' * ((FLOOR(gmt_offset) * 60 + (gmt_offset - gmt_offset::int) * 100)::int);
        END;
        $$ LANGUAGE plpgsql;
    </createProcedure>
</changeSet>

</databaseChangeLog>